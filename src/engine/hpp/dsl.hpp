#ifndef ENGINE_DSL_ON
#define ENGINE_DSL_ON
//--------------------------------------------------


#define PLAYER_X (player.motion.x)
#define PLAYER_Y (player.motion.y)

#define PLATFORM_X (platform.motion.x)
#define PLATFORM_Y (platform.motion.y)

#define ENGINE_FIELD_WIDTH  (engine->data.field_width)
#define ENGINE_FIELD_HEIGHT (engine->data.field_height)


//--------------------------------------------------
#endif